# Руководство по использованию Multi-Agent System

## Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка API ключа

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и добавьте ваш OpenRouter API ключ:

```env
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь
```

Получить ключ можно на [OpenRouter.ai](https://openrouter.ai/)

### 3. Проверка конфигурации

```bash
npm run create config
```

Эта команда проверит, правильно ли настроен ваш API ключ.

### 4. Создание первого проекта

#### Интерактивный режим (рекомендуется)

```bash
npm run create create
```

Система задаст вам вопросы:
- Идея проекта
- Дополнительные требования (опционально)

#### Режим с параметрами

```bash
npm run create create --idea "Создай todo приложение с аутентификацией" --requirements "Добавь темную тему и экспорт в PDF"
```

## Процесс создания проекта

Система автоматически выполнит следующие этапы:

### 1. 📋 Product Manager
- Анализирует вашу идею
- Создает детальную спецификацию
- Определяет features и user stories
- Формулирует бизнес-цели

### 2. 🎨 Designer
- Разрабатывает дизайн-систему
- Определяет цветовую палитру
- Подбирает типографику
- Создает список компонентов
- Проектирует layouts

### 3. 💻 Developer
- Пишет полный код проекта
- Создает структуру Next.js
- Настраивает Prisma схему
- Создает API endpoints
- Реализует UI компоненты

### 4. 🔍 Code Reviewer
- Проверяет код на безопасность
- Находит баги и проблемы
- Оценивает качество архитектуры
- Дает рекомендации

Если найдены проблемы:
- Developer автоматически исправляет замечания
- Повторная проверка (до 3 раз)

### 5. 📦 Финализация
- Сохранение проекта в `./generated-projects/`
- Создание документации
- Инструкции по установке

## Структура созданного проекта

```
generated-projects/
└── your-project-name/
    ├── app/                  # Next.js App Router
    │   ├── page.tsx
    │   ├── layout.tsx
    │   └── api/              # API Routes
    ├── components/           # React компоненты
    ├── lib/                  # Утилиты и конфигурация
    ├── prisma/              # Prisma схема
    │   └── schema.prisma
    ├── public/              # Статические файлы
    ├── package.json
    ├── tsconfig.json
    ├── tailwind.config.js
    ├── README.md            # Документация проекта
    └── SETUP.md             # Инструкции по установке
```

## Запуск созданного проекта

```bash
cd generated-projects/your-project-name
npm install
npx prisma generate
npx prisma migrate dev
npm run dev
```

Откройте http://localhost:3000

## Примеры использования

### Пример 1: Todo приложение

```bash
npm run create create --idea "Создай todo list приложение с возможностью создания, редактирования и удаления задач. Добавь фильтры по статусу."
```

### Пример 2: Blog платформа

```bash
npm run create create --idea "Блог с markdown редактором, комментариями и системой тегов" --requirements "Добавь dark mode и RSS ленту"
```

### Пример 3: E-commerce

```bash
npm run create create --idea "Простой интернет-магазин с корзиной, каталогом товаров и оформлением заказа"
```

## Настройка системы

### Базовые настройки

Отредактируйте `.env`:

```env
# API ключ для всех агентов
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь

# Общая модель по умолчанию
DEFAULT_MODEL=openai/gpt-4-turbo
```

### Индивидуальные модели для агентов

Вы можете настроить отдельные модели для каждого агента. Это полезно, если:
- Вы хотите использовать более быстрые модели для простых задач
- Нужно оптимизировать стоимость (использовать gpt-3.5-turbo где возможно)
- Разные задачи требуют разной мощности моделей

```env
# Общий ключ для всех агентов
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ

# Общая модель (используется, если не указана индивидуальная)
DEFAULT_MODEL=openai/gpt-4-turbo

# Индивидуальные модели для каждого агента (опционально)
MODEL_PRODUCT_MANAGER=openai/gpt-4-turbo
MODEL_DESIGNER=openai/gpt-3.5-turbo
MODEL_DEVELOPER=openai/gpt-4-turbo
MODEL_REVIEWER=openai/gpt-4-turbo
```

**Примеры конфигураций:**

1. **Оптимизация по скорости и стоимости:**
```env
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ
DEFAULT_MODEL=openai/gpt-4-turbo
MODEL_DESIGNER=openai/gpt-3.5-turbo      # Быстрее и дешевле для дизайна
MODEL_DEVELOPER=openai/gpt-4-turbo        # Мощная модель для кода
MODEL_REVIEWER=openai/gpt-4-turbo         # Мощная модель для review
```

2. **Все агенты используют одну модель:**
```env
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ
DEFAULT_MODEL=openai/gpt-4-turbo
# Индивидуальные модели не указаны - все используют DEFAULT_MODEL
```

3. **Проверка конфигурации:**
```bash
npm run cli config
```

Эта команда покажет, какие модели используются для каждого агента.

### Изменение директории для проектов

```env
OUTPUT_DIR=./my-projects
```

### Изменение количества ревизий

В `src/core/ProjectManager.ts` измените:

```typescript
private maxRevisions: number = 3; // Измените на нужное значение
```

## Логирование и мониторинг

Система автоматически логирует:
- Все сообщения между агентами
- Прогресс выполнения задач
- Ошибки и предупреждения
- Статистику коммуникаций

В конце работы выводится детальная статистика.

## Troubleshooting

### Ошибка "OPENROUTER_API_KEY не установлен"

**Решение**: Проверьте файл `.env` и убедитесь, что ключ добавлен правильно.

### Ошибка "OpenRouter API error: 401"

**Решение**: Ваш API ключ недействителен. Проверьте ключ на openrouter.ai

### Ошибка "OpenRouter API error: 429"

**Решение**: Превышен лимит запросов. Подождите или обновите тарифный план.

### Проект создан, но не компилируется

**Решение**:
1. Проверьте `SETUP.md` в созданном проекте
2. Убедитесь, что установлены все зависимости
3. Проверьте версию Node.js (требуется >= 18)

### Агенты долго отвечают

**Решение**:
1. Используйте более быструю модель (gpt-3.5-turbo)
2. Упростите требования
3. Проверьте соединение с интернетом

## Расширенное использование

### Использование в коде

```typescript
import { ProjectManager } from './src/core/ProjectManager';

const manager = new ProjectManager();

await manager.createProject({
  idea: 'Ваша идея',
  additionalRequirements: 'Дополнительные требования',
  techStack: {
    frontend: 'Next.js',
    backend: 'Next.js API Routes',
    database: 'PostgreSQL',
    orm: 'Prisma',
  },
});
```

### Кастомизация агентов

Вы можете изменить system prompts агентов в файлах:
- `src/agents/ProductManager.ts`
- `src/agents/Designer.ts`
- `src/agents/Developer.ts`
- `src/agents/CodeReviewer.ts`

## Лучшие практики

1. **Четкие требования**: Чем детальнее вы опишете идею, тем лучше результат
2. **Итеративный подход**: Начните с простой идеи, затем усложняйте
3. **Проверка результатов**: Всегда проверяйте созданный код перед использованием в продакшене
4. **Доработка**: Созданный проект - это foundation, который вы можете улучшить

## Ограничения

1. Система создает foundation проекта, но не production-ready приложение
2. Требуется ручная доработка для специфичных требований
3. Зависит от качества ответов LLM
4. Ограничена возможностями выбранной модели

## Поддержка и развитие

Для вопросов и предложений:
- GitHub Issues
- Pull Requests приветствуются

## Лицензия

MIT
