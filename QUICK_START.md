# 🚀 Быстрый старт

## Шаг 1: Настройка API ключа

1. Получите API ключ на [OpenRouter.ai](https://openrouter.ai/keys)

2. Создайте файл `.env`:
   ```bash
   cp .env.example .env
   ```

3. Откройте `.env` и добавьте ваш ключ:
   ```env
   OPENROUTER_API_KEY=sk-or-v1-ваш-ключ-здесь
   DEFAULT_MODEL=openai/gpt-4-turbo
   ```
   
   **Опционально:** Вы можете настроить отдельные модели для каждого агента:
   ```env
   OPENROUTER_API_KEY=sk-or-v1-ваш-ключ
   DEFAULT_MODEL=openai/gpt-4-turbo
   MODEL_DESIGNER=openai/gpt-3.5-turbo      # Быстрее для дизайна
   MODEL_DEVELOPER=openai/gpt-4-turbo        # Мощная модель для кода
   ```

## Шаг 2: Проверка конфигурации

```bash
npm run create config
```

Должно показать:
```
🔧 Конфигурация:
API Key: ✓ Установлен
✅ Конфигурация в порядке!
```

## Шаг 3: Создайте первый проект

```bash
npm run create create
```

Введите:
- **Идея**: `Создай простое todo приложение`
- **Требования**: `(оставьте пустым или добавьте свои)`

## Что произойдет дальше?

Система запустит 5 агентов, которые:

1. 📋 **PM** создаст спецификацию (~30 сек)
2. 🎨 **Designer** разработает дизайн-систему (~30 сек)
3. 💻 **Developer** напишет код (~2-3 мин)
4. 🔍 **Reviewer** проверит код (~1 мин)
   - Если найдены проблемы → исправления → повторная проверка
5. 📦 Проект сохранится в `./generated-projects/`

**Общее время**: 4-6 минут

## Шаг 4: Запустите созданный проект

```bash
cd generated-projects/ваш-проект
npm install
npx prisma generate
npm run dev
```

Откройте http://localhost:3000

## Примеры идей для первых проектов

### Простые (5-10 мин)
```bash
npm run create create --idea "Todo list приложение"
npm run create create --idea "Блог с постами и комментариями"
npm run create create --idea "Список контактов с поиском"
```

### Средние (10-15 мин)
```bash
npm run create create --idea "Книжный каталог с рейтингами"
npm run create create --idea "Трекер привычек"
npm run create create --idea "Менеджер заметок с тегами"
```

### Сложные (15-20 мин)
```bash
npm run create create --idea "Интернет-магазин с корзиной"
npm run create create --idea "Social media платформа"
npm run create create --idea "Dashboard с аналитикой"
```

## Советы

1. **Начните с простого** - первый проект должен быть простым для проверки работы
2. **Четкие требования** - чем детальнее описание, тем лучше результат
3. **Проверяйте лимиты** - убедитесь что на вашем API ключе достаточно кредитов
4. **Терпение** - первая генерация может занять 5-7 минут

## Troubleshooting

### Ошибка 401 (Unauthorized)
**Проблема**: Неверный API ключ
**Решение**: Проверьте `.env` файл и ключ на openrouter.ai

### Ошибка 429 (Rate Limit)
**Проблема**: Превышен лимит запросов
**Решение**: Подождите или обновите тарифный план

### Долго генерируется
**Решение**:
- Используйте более быструю модель: `DEFAULT_MODEL=openai/gpt-3.5-turbo`
- Упростите идею проекта

### Код не компилируется
**Решение**:
- Проверьте Node.js версию (требуется >= 18)
- Переустановите зависимости: `rm -rf node_modules && npm install`

## Дальше

- Прочитайте [USAGE.md](./USAGE.md) для детального руководства
- Изучите код в `src/agents/` для понимания работы агентов
- Кастомизируйте system prompts под ваши нужды

---

**Готовы создавать проекты? Запускайте `npm run create create`!** 🚀
